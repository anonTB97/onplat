// The persona council: six sequential passes over the codebase as it stands
// (architect, ATO/RMF, data & performance, naval UX/HCI, reliability/QA,
// product/domain), a naval architect's hull-grid template alongside, a
// synthesis into one prioritised backlog, and two adversarial verifiers.
// Every pass writes docs/council/<n>-<slug>.md and commits it, so a cut-off
// loses nothing. Run: Workflow({scriptPath: "tools/programme/council.js"}).
export const meta = {
  name: 'persona-council',
  description: 'Six persona passes over the codebase, a hull-grid template, one merged backlog, adversarial verification',
  phases: [
    { title: 'Council', detail: 'sequential persona passes; each reads the earlier ones and names its tensions' },
    { title: 'Synthesis', detail: 'one prioritised backlog: ATO-blocking, crash/perf-blocking, UX/polish' },
    { title: 'Verify', detail: 'skeptics on the control-ID claims and the performance numbers' },
  ],
}

const REPO = '/home/user/shipyardaionboard'

const COMMON = [
  'Repository: ' + REPO + ' on branch claude/kickoff-from-docs-arhiib (Rust cargo workspace under crates/, React/Vite shell under shell-web/, PostgreSQL migrations under migrations/, ops scripts under scripts/ and deploy/, design packets under docs/programme/, the standing posture in docs/production-posture.md, the ATO material in docs/ato-package.md, docs/ssp-input.md (generated by `cargo run -p xtask -- gen-ssp`, never hand-edited), docs/poam.md, docs/identity-proxy-contract.md, docs/runbook.md, docs/pilot-playbook.md, docs/demo-script.md, docs/execution-plan.md (what has landed, by slice), docs/zone-scheme.md, docs/stress-test.md).',
  'The product: availability planning and work authorization for naval shipyards; decision support only (never edits the schedule of record, never grants an authorization); the engine (crates/wadl-engine) is pure and wasm-safe; identity enters in one place (crates/wadl-api/src/auth.rs: dev header shim by default, proxy-asserted person/roles/org/hulls when WADL_PROXY_KEY is set, capability matrix in roles.rs); the audit ledger is append-only and hash-chained with the actor in the hash (chain format 2); row-level security on every tenant table proved against real PostgreSQL (crates/wadl-store/tests/pg_rls.rs); documents enter through doors (dry run, typed refusals, commit ledgered, revert); middleware is hand-rolled in crates/wadl-api/src/hardening.rs on tokio (no tower-http); logging is hand-rolled JSON lines (audit on stdout, diagnostics on stderr); CI (.github/workflows/ci.yml) runs fmt/clippy/test, leak tests, wasm builds, migrations + RLS on PostgreSQL, a production-path job (bootstrap, load-docs, drill, /health), cargo-deny, SBOM, vendored offline build, a STIG-style self-assessment (scripts/self-assessment.sh); the demo boots the reference hull (reference/cvn73: 476 spaces, 5,706 activities) through the doors; the dev box has 4 CPUs, 15 GB RAM, about 16 GB of free disk, PostgreSQL 16 on 127.0.0.1:55433 (database wadl, user postgres, no password), a release binary at target/release/serve (rebuild with `CARGO_INCREMENTAL=0 cargo build --release -p wadl-api --bin serve`; another agent may be editing crates/wadl-api/src/rule_table.rs, so if the tree does not compile, wait a few minutes and retry rather than touching that file), Chromium at /opt/pw-browsers/chromium with playwright-core in shell-web/node_modules.',
  'Rules for your pass: review what is actually there before writing a word; cite file paths and line numbers for every claim; distinguish what is solid, what is missing, and what is actively disqualifying for an IL5/NNPI accreditation or for carrier-scale concurrent use; do not propose a rewrite unless something is architecturally disqualifying, and say why; no reassurance language, no marketing summaries. Read the council documents written before yours under docs/council/ and, where your requirements are in tension with an earlier persona, say so explicitly and propose a resolution rather than picking one silently. You do not implement anything in this pass except where your mandate says to run or measure; you produce the assessment, the prioritised action list with effort and blocking class (ATO-blocking, crash/perf-blocking, UX/polish), and the concrete change list (file, change, why) an implementer can execute without asking.',
  'Write your document to docs/council/<your file>, sections: 1 Current state (solid / missing / disqualifying, each with evidence); 2 Prioritised actions (a table: id, action, blocking class, effort in agent-hours, depends on); 3 Concrete changes (file, change, why); 4 Tensions with earlier personas and proposed resolutions; 5 Questions only the customer can answer. Keep it under 400 lines. Then commit it with `git add docs/council/<your file>` (never `git add -A`; other agents have uncommitted work), the message headline in the codebase\'s plain voice, and exactly these two trailer lines at the end of the message: "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" and "Claude-Session: https://claude.ai/code/session_01YRBej8TEXhBQGoSv6HkkhA"; author with `-c user.name="Claude" -c user.email="noreply@anthropic.com"`; push with `git push -u origin claude/kickoff-from-docs-arhiib` retrying on network errors. Return the JSON index.',
].join('\n')

const PERSONAS = [
  { n: 1, slug: 'architect', file: '1-chief-systems-architect.md', who: 'Chief Systems Architect: containerization and platform', mandate: 'Get this into a container posture that survives an ATO review and does not fall over under real load. Assess and specify: multi-stage Docker builds with minimal base images (distroless or hardened slim), no root processes, no baked-in secrets; docker-compose for local dev; a clear path to Kubernetes or a DoD-approved platform (Platform One / Big Bang, or equivalent on-prem orchestration) for production, without assuming cloud-native SaaS patterns that will not survive an air-gapped or IL5-boundary deployment; externalized config via environment or a secrets manager, never files in the image; health checks, readiness and liveness probes, graceful shutdown, horizontal scalability of the app tier; a real story for the data tier under concurrent multi-team writes (PostgreSQL with RLS exists: assess connection pooling, transaction isolation, write conflicts, the in-memory store\'s role); document the deployment topology assumption (on-prem, GovCloud, air-gapped) and how IL5/NNPI constraints differ by which one it is. Read deploy/, scripts/, .github/workflows/ci.yml, crates/wadl-api/src/bin/serve.rs, hardening.rs, lib.rs, crates/wadl-store/src/pg.rs and pg_repo.rs, Cargo.toml files, shell-web/package.json and vite config, docs/runbook.md, docs/production-posture.md. Your concrete changes are the Dockerfile(s), compose file, Kubernetes manifests or Helm values outline, probe endpoints, shutdown handling, and config contract, specified precisely enough to implement.' },
  { n: 2, slug: 'ato', file: '2-ato-rmf-compliance.md', who: 'ATO / RMF Compliance Lead: IL5 and NNPI posture', mandate: 'This has to survive a real accreditation package. Map the current architecture against NIST 800-53 control families (AC, AU, SC, SI, IA at minimum; CM and CP where the repo already claims them) and flag gaps by control ID, not vibes: read docs/ssp-input.md, docs/production-posture.md, docs/ato-package.md, docs/poam.md, docs/identity-proxy-contract.md, xtask/src (gen-ssp and its template), scripts/self-assessment.sh, crates/wadl-api/src/{auth.rs, roles.rs, hardening.rs, lib.rs, bin/serve.rs}, crates/wadl-store/src/{ledger.rs, pg.rs, pg_repo.rs}, migrations/0001 and 0007 and 0017, the audit line format, deploy/. NNPI is a distinct, stricter bar than IL5: treat any Naval Nuclear Propulsion Information as requiring need-to-know access control, not just role-based access; call out where NNPI-marked records would need to be segregated or excluded from broader team visibility entirely (the reference hull has reactor compartments marked restricted in docs/zone-scheme.md; the schedule of record, hazards and the register could carry NNPI); design both readings (in-app NNPI data with a need-to-know marking and compartment-level segregation enforced in RLS and in the shell, versus metadata-only references with the NNPI content held outside) and state plainly that the customer must confirm scope before either is assumed. Assess encryption at rest (FIPS 140-2/140-3 validated modules: what the binary uses today for hashing and TLS, what PostgreSQL at rest needs, what the container needs), encryption in transit (TLS 1.2+, no weak ciphers: who terminates TLS today, the proxy assumption), STIG-aligned hardening for base OS and container images, audit logging that is tamper-evident and covers authentication, authorization decisions and data access (not just errors: read what the audit middleware logs on every request, what the ledger records, and where data reads are not logged), identity (CAC/PIV-ready path even if stubbed, no plaintext credential storage, session handling meeting DoD policy: what the proxy contract promises and what the app enforces). Produce the gap list formatted as a POA&M starting point: control ID, current state with evidence, remediation, rough effort, blocking class.' },
  { n: 3, slug: 'perf', file: '3-data-and-performance.md', who: 'Data & Performance Engineer: CVN-scale without crashing', mandate: 'This has to hold up against carrier-scale scheduling data under real concurrent use. Do not estimate from the reference hull: GENERATE a synthetic CVN-scale dataset and RUN it. Use tools/gen_cvn73_hull.py and tools/gen_full_xer.py (read them; add --scale and --out flags if they lack them, committing only the generator changes, never the generated data: write it under /tmp/claude-0/-home-user-shipyardaionboard/05fc1137-a85d-5901-a9e2-65d3eeb86153/scratchpad/scale/); targets: at least 3,000 compartments, 30,000 to 60,000 activities, a multi-year availability, dense predecessor edges and couplings, several hundred live hazards. Boot the release binary on it (WADL_DEMO_DOCS=<dir> WADL_SCHEDULE_XER=<xer> WADL_PORT=8080 target/release/serve; rebuild release first with CARGO_INCREMENTAL=0 if the tree compiles; if it does not because another agent is mid-edit, wait and retry) and measure with curl and a small Node or bash load script (hand-rolled; no new dependency): boot time and memory (RSS), then per endpoint p50/p95 at 1 and at 10 concurrent clients: /deck-states, /activities, /issues, /leverage, /schedule-alternatives, /zones/Z4/adjacent, /compartments/:no/state, /ledger, /schedule-runs/diff, a schedule-of-record dry run and commit of the big XER, a hazard clear during reads; also load the same dataset into PostgreSQL through `wadl bootstrap-hull` + `wadl load-docs` (build the CLI with --features postgres) and repeat the key reads there. Identify N+1 queries, unindexed lookups, per-request full recomputation (the engine evaluates per compartment per read; memoisation exists for some paths), unbounded serialisation, and in the shell (read SequenceBoard.tsx, DeckExplorer.tsx, DailyOps.tsx, Reports.tsx, ZoneLanes.tsx, App.tsx: what loads the whole activity list into memory, what renders unbounded, what should be paginated, virtualised or server-filtered). Define the real-time strategy for concurrent users and justify it against multi-team concurrent access (SSE with backpressure, polling with sane intervals keyed on the ledger sequence or a data epoch, or websockets; the codebase has a dataEpoch pattern in the shell and a ledger seq on the server: build on those). Define graceful degradation: partial data with a visible notice, never a hung tab. Write docs/council/3-data-and-performance.md with the measured numbers in tables (never a number you did not measure), then update docs/stress-test.md with the same numbers and the reproduction commands, add the load script under scripts/ (e.g. scripts/load-test.sh or tools/load_test.mjs) and commit those with your document. Your concrete change list: the indexes, caches, pagination, virtualisation and the update channel, each with the measurement that justifies it and the target number after.' },
  { n: 4, slug: 'ux', file: '4-naval-ux-hci.md', who: 'Naval UX/HCI Lead: an advanced, intuitive interface for shipyard users', mandate: 'This has to be usable by planners, supervisors and field personnel under real working conditions. Design for the actual users: schedule planners at a desk, deck-plate supervisors who need fast status checks, QA/NDT reviewing completion evidence, hot-work/fire-watch needing deconfliction status at a glance; different roles need different default views of the same data (the shell has seven personas with landing screens: Chrome.tsx PERSONAS; assess whether QA/NDT and fire watch exist as roles and what they lack). Prioritise information density and scan-ability; Gantt/timeline (SequenceBoard lanes), deck/compartment spatial views (DeckExplorer, ShipView), and dependency/deconfliction visualisations (CascadeBoard, LeverageBoard, IssuesBoard) must be fast to read and support drill-down without reloads. Explicit, unmistakable, consistent states for schedule conflicts, blocked/held work and anything safety-relevant (hot-work adjacency, fire-watch holds): no ambiguity permitted where safety or schedule-integrity stakes exist (read docs/programme/s20-vocabulary-polish-and-onboarding.md, which already audited the badge primitive, the words and the legend: do not redo that audit; build on it and say what it missed). Design for degraded conditions: small screens, gloved or imprecise touch, intermittent connectivity near the work site (assess the shell at 390 px and 768 px widths with Playwright screenshots of the demo path; read what happens on a failed fetch). Accessibility and keyboard/mouse efficiency for desk-bound planners (focus order, Escape, arrow keys, shortcuts, contrast, reduced motion). Run the shell against the release binary on the reference hull and LOOK at the screens at three widths before writing; screenshots to the scratchpad. Your concrete changes: role views, state vocabulary, responsive and touch rules, keyboard model, offline/degraded behaviour, each as file-level changes; name the tension between field glanceability and desk density and resolve it (e.g. one density token with two settings, not two products).' },
  { n: 5, slug: 'qa', file: '5-reliability-and-qa.md', who: 'Reliability/QA Lead: "does not crash" as an engineering property', mandate: 'Turn "unparalleled functionality, not crashing" into test coverage and failure-mode handling. Assess error boundaries and graceful failure at every layer (React error boundaries in App.tsx? per-panel? what a bad record, a slow query or a dropped connection does today; the "unavailable" convention and where it is missing), structured logging and error reporting a support team could use (the audit and diagnostic JSON lines, request ids, the support bundle, what leaks: does any log line carry compartment names, person names or schedule content that could be NNPI or IL5-sensitive?), the test pyramid as it stands (count and classify: unit, property, golden/insta, API integration on memory and PostgreSQL, leak tests, RLS proof, the shell vitest suites, the Playwright smoke designed in docs/programme/s17-proof-and-cold-walkthrough.md but not yet built), and define and specify tests for explicit failure modes: auth token or proxy session expiry mid-session, a write conflict when two users act on the same record (the ledger is append-only; hazards clear/raise, issue decisions, document commits: what happens today under two concurrent commits, on both stores), a backend outage mid-operation, a partial document load, a database behind the binary at boot, disk full on the audit stream. Each needs a defined behaviour and a test, not undefined behaviour. Your concrete changes: the error-boundary components, the request-id and correlation contract, the log redaction rules, the concurrency tests on PostgreSQL, the end-to-end tests for the critical multi-team workflows (a planner proposes, a supervisor sees it update, a conflict gets flagged, a safety officer clears and the board re-derives), and the chaos-style tests (kill the database under load, throttle the network) as scripts under scripts/ or tests/.' },
  { n: 6, slug: 'product', file: '6-product-domain-owner.md', who: 'Product/Domain Owner: single source of truth across teams', mandate: 'Keep the build honest against what "single source of truth for many teams" requires; it is a data-governance problem as much as a technical one. Define the authoritative data model and ownership: which team\'s system of record feeds which fields (P6 the schedule; the yard\'s register and drawings the hull; the safety authority the rule table; the daily log the hazards; the proxy the people), what happens when two teams\' views of the same work item disagree (the run history, the quarantine, the proposals loop), who has write access to what (the capability matrix in crates/wadl-api/src/roles.rs; map it to real shipboard and shipyard roles: planning, hot-work/fire-watch, NDT/QA, deck-plate supervision, ship\'s force, the isolation authority, the fire marshal; say which roles are missing). Every change to a shared record needs an audit trail (who, what, when, previous value) visible to relevant teams: check what the ledger records per action kind and where "previous value" is absent. Flag anywhere the current build has any team\'s data as a local copy or export rather than a live reference to the source of truth (browser localStorage, CSV exports, the in-memory store, the seed world, the shell\'s derived figures), because that is the pattern most likely to break the premise. Read docs/council/7-hull-grid-template.md (the naval architect\'s implementation template for zones and compartments) and place it in the ownership model: the hull grid is authored once per program per vessel at implementation and everything spatial derives from it. Your concrete changes: the ownership matrix as a document (docs/data-ownership.md outline), the role and capability additions, the ledger detail additions (previous value), and the removal or labelling of every local copy.' },
]

const HULL_GRID = { n: 7, slug: 'hullgrid', file: '7-hull-grid-template.md', who: 'Naval architect and implementation lead: the hull grid as the one-time template for zones and compartments', mandate: 'The customer\'s direction: when a customer asks how the system knows where the zones are, the answer is manual work up front, once per program per vessel: the implementers establish the grid frame by frame (frame 1, 2, 3 … bow to stern), transverse port and starboard, deck by deck, and zone by zone, and the system then auto-populates compartments and zones from that grid. Design that process as a naval architect would and as the implementation template for every future vessel. Read docs/zone-scheme.md (zones are 3-D blocks: a frame band on a band of decks; blocks partition every deck), reference/cvn73/*.csv (the register, zones, geometry, couplings as they exist), crates/wadl-api/src/documents.rs and the zone handlers (zone audit, adjacency), crates/wadl-domain/src/compartment.rs (the USN compartment number: deck-frame-side-usage), shell-web/src/{zones.ts, ShipView.tsx, DeckExplorer.tsx, deckGeometry.ts, deckSheets.ts}, tools/gen_cvn73_hull.py. Specify: the hull grid document (frame table: frame number, longitudinal station or spacing with spacing changes, the forward perpendicular and after perpendicular; deck table: deck code, ordinal, name, height; transverse bands: port/starboard/centreline and the USN side-digit convention; the zone chart drawn on that grid as blocks with owners); how compartments are auto-placed from their USN numbers against the grid and validated (a compartment whose frame is outside the hull, whose deck code is unknown, whose side digit contradicts its zone) with the refusals and findings the door should emit; what the implementer does by hand (the grid, the zone chart, the exceptions) and what the system derives (compartment placement, zone membership, adjacency, the plates\' frame scale); the CSV shapes as a template a yard fills in (with a filled example for CVN-73 drawn from the reference files), the implementation checklist for a new vessel (what to collect, in what order, the dry-run findings to expect, sign-off), and how the existing zone chart and geometry documents converge on the grid without breaking what is stored. Write docs/council/7-hull-grid-template.md and a design packet docs/programme/s22-hull-grid.md in the packet layout the other packets use (Summary; What already exists; Scope; Out of scope; Contracts; Files; Tests; Acceptance; Demo moment; Depends on / conflicts with; Risks; Needs from the yard; Estimate; under 350 lines), commit both (explicit git add of the two files) and push.' }

const INDEX_SCHEMA = {
  type: 'object', required: ['persona', 'doc_path', 'solid', 'missing', 'disqualifying', 'top_actions', 'tensions', 'questions_for_customer'],
  properties: {
    persona: { type: 'string' }, doc_path: { type: 'string' },
    solid: { type: 'array', items: { type: 'string' } },
    missing: { type: 'array', items: { type: 'string' } },
    disqualifying: { type: 'array', items: { type: 'string' } },
    top_actions: { type: 'array', items: { type: 'object', required: ['id', 'action', 'blocking_class', 'effort_hours'], properties: { id: { type: 'string' }, action: { type: 'string' }, blocking_class: { type: 'string', enum: ['ato', 'crash_perf', 'ux_polish'] }, effort_hours: { type: 'number' }, depends_on: { type: 'array', items: { type: 'string' } } } } },
    tensions: { type: 'array', items: { type: 'object', required: ['with', 'what', 'resolution'], properties: { with: { type: 'string' }, what: { type: 'string' }, resolution: { type: 'string' } } } },
    questions_for_customer: { type: 'array', items: { type: 'string' } },
    measurements: { type: 'array', items: { type: 'object', required: ['what', 'value'], properties: { what: { type: 'string' }, value: { type: 'string' } } } },
  },
}

function personaPrompt(p, earlier) {
  return [
    'You are the ' + p.who + ' on a six-persona council reviewing a naval-shipyard work-authorization product end to end before it goes to a containerized, ATO-track state for IL5 and NNPI handling, serving multiple teams (planning, hot-work/fire watch, NDT/QA, deck-plate supervision) simultaneously against CVN-scale schedules (tens of thousands of line items, multi-year availabilities, dense dependency graphs).',
    'Mandate: ' + p.mandate,
    COMMON,
    'Your file: docs/council/' + p.file + '.',
    earlier.length ? 'Council documents written before yours (read them; name your tensions with them): ' + earlier.join(', ') : 'You are the first persona; the hull-grid template (docs/council/7-hull-grid-template.md) may or may not exist yet — read it if it does.',
  ].join('\n\n')
}

phase('Council')
log('Council: six sequential persona passes, the hull-grid template alongside')
const gridP = agent(personaPrompt(HULL_GRID, []), { label: 'hull-grid', phase: 'Council', schema: INDEX_SCHEMA, effort: 'high' })
const passes = []
const earlier = []
for (const p of PERSONAS) {
  const r = await agent(personaPrompt(p, earlier.slice()), { label: 'persona:' + p.slug, phase: 'Council', schema: INDEX_SCHEMA, effort: p.slug === 'ato' || p.slug === 'perf' ? 'xhigh' : 'high' })
  if (r) { passes.push(r); earlier.push('docs/council/' + p.file) }
  else { log('persona ' + p.slug + ' returned nothing (cut off?); the synthesis will read whatever it committed') }
}
const grid = await gridP

phase('Synthesis')
const backlog = await agent([
  'You are the programme lead synthesising the six persona passes and the hull-grid template into ONE prioritised backlog for a naval-shipyard work-authorization product going to a containerized, ATO-track IL5/NNPI state at CVN scale.',
  COMMON,
  'Read every document under docs/council/ (the six persona files and 7-hull-grid-template.md), docs/programme/programme.md and docs/execution-plan.md (what is already landed or in flight: S14 sitting B is being finished by another agent; S17, S18/S19, S20 and S22 are designed but not built).',
  'Write docs/council/backlog.md: (1) a one-paragraph current-state verdict per persona (solid / missing / disqualifying) in the personas\' own words, no softening; (2) the merged backlog in three sections with different urgency — ATO-blocking, crash/performance-blocking, UX/polish — each item with id, source personas, current state with evidence, the change, effort in agent-hours, depends-on, and which existing slice or packet it lands in or whether it is new; ordered within each section by what unblocks the most; (3) every tension between personas, stated explicitly, with the resolution chosen and why (real-time responsiveness vs audit-log-everything; field glanceability vs desk density; containerized statelessness vs the in-memory store; NNPI segregation vs single-source-of-truth visibility; graceful degradation vs "never render good news you cannot prove"); (4) the first tranche: the ordered list of items to build now (the ATO-blocking and crash/perf-blocking items that need no customer answer), each with the exact files and the acceptance check; (5) the questions only the customer can answer (NNPI scope in-app vs metadata-only, deployment topology, TLS termination ownership, the roles missing), each with the default the build assumes until answered. Commit it (explicit git add docs/council/backlog.md; the commit format from the persona instructions) and push. Return the JSON index.',
  'Persona indexes: ' + JSON.stringify(passes),
  'Hull grid index: ' + JSON.stringify(grid),
].join('\n\n'), { label: 'synthesis', phase: 'Synthesis', schema: { type: 'object', required: ['doc_path', 'first_tranche', 'ato_blocking_count', 'crash_perf_count', 'ux_polish_count', 'questions'], properties: { doc_path: { type: 'string' }, first_tranche: { type: 'array', items: { type: 'object', required: ['id', 'title', 'files', 'acceptance', 'effort_hours'], properties: { id: { type: 'string' }, title: { type: 'string' }, files: { type: 'array', items: { type: 'string' } }, acceptance: { type: 'string' }, effort_hours: { type: 'number' } } } }, ato_blocking_count: { type: 'number' }, crash_perf_count: { type: 'number' }, ux_polish_count: { type: 'number' }, questions: { type: 'array', items: { type: 'string' } } } }, effort: 'xhigh' })

phase('Verify')
const VERDICT = { type: 'object', required: ['lens', 'doc_path', 'refuted', 'confirmed', 'corrections'], properties: { lens: { type: 'string' }, doc_path: { type: 'string' }, refuted: { type: 'array', items: { type: 'object', required: ['claim', 'evidence'], properties: { claim: { type: 'string' }, evidence: { type: 'string' } } } }, confirmed: { type: 'array', items: { type: 'string' } }, corrections: { type: 'array', items: { type: 'string' } } } }
const verifiers = (await parallel([
  function () { return agent(['You are an accreditation assessor trying to REFUTE the control-ID claims in docs/council/2-ato-rmf-compliance.md and the ATO-blocking section of docs/council/backlog.md. For every claim that a control is implemented, partially implemented or missing, go to the cited file and line and check it; default to refuted when the evidence is not there. Also check the two claims most likely to be wrong in a Rust/axum system: that the audit trail covers data reads, and that hashing/TLS uses FIPS-validated modules (what does the binary actually link: read Cargo.lock for the hash and TLS crates; is TLS terminated in-process at all?). Write docs/council/verify-ato.md (refuted claims with evidence, confirmed claims, corrections to the backlog), commit it (explicit git add; the persona commit format with the two trailer lines) and push. Return the JSON.', COMMON].join('\n\n'), { label: 'verify:ato', phase: 'Verify', schema: VERDICT, effort: 'xhigh' }) },
  function () { return agent(['You are a performance engineer trying to REFUTE the numbers and conclusions in docs/council/3-data-and-performance.md and docs/stress-test.md. Re-run at least three of the reported measurements yourself with the reproduction commands given (the release binary on the scale dataset under the scratchpad scale/ directory if it still exists, else regenerate with the generator flags the document names); report yours beside theirs; check the shell claims by reading the named files; check that the proposed real-time strategy actually survives 50 concurrent clients on this box (measure). Default to refuted when a number cannot be reproduced within 2x. Write docs/council/verify-perf.md, commit it (explicit git add; the persona commit format with the two trailer lines) and push. Return the JSON.', COMMON].join('\n\n'), { label: 'verify:perf', phase: 'Verify', schema: VERDICT, effort: 'high' }) },
])).filter(Boolean)

return { passes: passes, grid: grid, backlog: backlog, verifiers: verifiers }
