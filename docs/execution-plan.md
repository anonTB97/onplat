# Execution plan — from the reviews to a demoable, production-shaped build

Companion to `docs/pilot-readiness-review.md` and `docs/ux-gap-analysis.md`.
Those say what is wrong and why. This says what is being built, in what
order, and the rules every slice is held to. It is updated as slices land.

## The objective

A complete, demoable build — not pilot-ready, but real: every screen answers
a real person's question without a tour guide, every number traces to a
document or the engine, nothing on screen is a mock — built so that the path
to a Navy production deployment (an ATO, a yard's identity proxy, a
PostgreSQL behind row-level security) is a matter of configuration and
evidence, not a rewrite.

## The rules every slice is held to

1. **No new runtime dependency without a written reason.** The hand-rolled
   posture (middleware, compression, static serving, audit, ledger, parsers)
   is the accreditation story. A dependency is added only when the
   alternative is a worse security argument, and the reason goes in the
   commit message.
2. **Every fact carries its provenance, and every derived figure says so.**
   Man-hours come from the schedule of record; verdicts from the engine;
   estimates from the shell. A screen or a sheet that shows a figure names
   its layer.
3. **Nothing renders good news it cannot prove.** A failed read is
   "unavailable", never an empty list that reads as clearance.
4. **The past does not change because somebody acted in the present.** A
   clearance, an acknowledgement, a decision has its own instant; a board
   scrubbed back shows what was really there.
5. **The lint gate is CI's gate.** `cargo fmt --all` then
   `RUSTFLAGS="-D warnings" cargo clippy --workspace --all-targets --all-features -- -D warnings`
   then `cargo test --workspace --all-features`; `npx tsc -b`, `npx vitest run`,
   `npx vite build` for the shell. Nothing is pushed red.
6. **Each slice is pushed on its own**, with tests that pin the behaviour it
   adds, so the branch is always demoable at its head.

## The slices

| # | Slice | Status | What it delivers |
|---|---|---|---|
| 1 | **The front door** | landed `ef9469f` | Rail grouped by the working day; screen names as headlines; seven yard roles with real landing screens, remembered per browser; one status strip with the same numbers on every screen; yard words for the four authorization states with a Legend panel; first-run cards; empty decks folded; failed reads rendered as "unavailable". |
| 2 | **Trust fixes** | landed `3318243` | Cleared hazards stay visible at instants before the clearance (both stores, `as_of` on the hazards endpoint, integration test); cascade never re-emits the origin (unit + property test); doubled USN usage codes parse; empty proxy key refuses to boot and cannot admit; unknown `/api` paths 404; acknowledgements stamped on the wall clock. |
| 3 | **Reports** | landed `37db174` | A Reports module: shift sheet, zone day sheet, compartment card, conflict log, field-condition register — pure builders (`reports.ts`, unit-tested), one screen, monochrome print, CSV with the cut in its header. Every sheet carries hull, instant, schedule source and producer, and names the layer of each figure. |
| 4 | **The ship through the product** | landed `2000cf7`, `36599d4` | Booked hours derived from the ingested schedule instead of seeded work orders (the readiness rollup says which, `hours_source`); a hazard-raise route validated against the register, raised from the deck plan beside the clearance. Three doors in the Data Sources home: the hull's compartment register (once stored it *is* the hull; findings name what would lose its space), the coupling register with derived vertical adjacency marked `derived`, and the daily hazard-log CSV (already-live rows skipped, each raise ledgered under its log). Every document commit and revert writes the ledger, on both stores. |
| 5 | **The yard's clock** | landed `3621485`, `a64da11`, `bb99a3f`, `458ec6b` (server); `df18c59`, `4e0f002`, `3eeb876` (shell) | The yard's clock as an authored document (IANA zone, standard offset, an optional daylight rule as two nth-weekday transitions, the watch length, the shifts by the yard's names), evaluated by a pure civil-time module in `wadl-domain` pinned to a shared vector file whose instants come from the IANA database; stored on both stores (migration 0016), loaded at boot from `reference/cvn73/CVN73-clock.csv` before the export, seeded as Norfolk for the 24-space world, honest UTC on PostgreSQL until a clock is loaded. A door (GET, dry run with this year's transitions and today's shifts as findings, commit, revert, ledgered `DOCUMENT_REPLACED`/`DOCUMENT_REVERTED` kind `yard_clock`, leak-tested, in the SSP). The XER's wall clock is read in the hull's clock; a start the clock skipped or repeated is accepted with a finding naming the line and the reading; the schedule of record remembers the clock it was parsed in and the clock door warns when it needs re-importing. `/timeframe` carries the clock. Shell: `yardClock.ts` mirrors `civil.rs` one-to-one and is pinned to the same vector file, with an `Intl` cross-check of the authored rule against the browser's tz database at four instants; `clock.ts` is the one place the clock is applied — every formatter renders the yard's wall clock, `Z` only under the UTC default, the record-grade stamp always carries the offset (`2026-09-04 09:15 −04:00`); the time strip names the zone once (`America/New_York · UTC−04:00`, amber `UTC · no yard clock loaded` under the default) and the day grid is the yard's watches, five hours long on the fall-back night and labelled so; Daily Ops and the Reports run on the calendar's shifts by the yard's names (`Days 0700–1530 · Swing 1530–2400 · Mids 0000–0700`, no `(Z)`), the shift sheet's footer names the clock once, the P6 change-request CSV and the register export write wall clock with the zone said; the Sources board has the Yard clock card with the door (upload → dry run with the server's and the browser's findings → confirm; revert to UTC) and the ledger names the document kind on its row. Deferred: week/month bucketing in Load digest and Schedule trace stays on UTC Monday/1st; the hazard log's `since` keeps requiring `Z`; `wadl-cli ingest-xer` parses UTC; the PG seed carries no clock row until B10; a "the yard's day starts at…" landing and Tomorrow mode (H3). |
| 6 | **Demo hardening** | landed `49af293` | The demo boots on the reference hull by default (`scripts/dev.sh`, release build; `WADL_DEMO=seed` for the seed); the issues and leverage reads are a third of a second and under a second on it (out-edge index on the adjacency graph, memoised schedule loads); `/health` asks the store and answers 503 when it cannot reach it, reporting the migration and document schema versions; every stored document is stamped `schema_version`; the two long boards page; a cold walk of every module renders clean; `docs/demo-script.md` is the scripted, role-by-role path over the hull. Deferred: running the API suite against PostgreSQL in CI and a CLI door for the documents — operator tooling the demo does not need (the store's RLS test still runs against a real PostgreSQL). |
| 7 | **The hull at scale, as documents** | landed `27ab339` | The zone scheme written down (`docs/zone-scheme.md`): zones are 3-D blocks — a frame band on a band of decks — and the chart, the audit and every view carry them. A generated CVN-73 hull at believable scale (`reference/cvn73`: 476 spaces on twelve decks, authored couplings with derived deck penetrations, geometry, a morning's log) and a schedule of record of 5,700 activities located to it; the demo boots on them through the doors' own paths (`WADL_DEMO_DOCS`). Vertical adjacency follows the deck order, not `ordinal + 1`. |
| 8 | **Zone focus with next-door awareness** | landed `10552cf` | A zone in focus blots out every other zone on the plate, the whole-ship view and the register — and keeps next-door work visible: spaces across the frame boundary, on the deck above or below, or coupled into the zone, each saying why. Served once from the register, chart and couplings (`/zones/:zone/adjacent`); the Zone Manager lands in it. |
| 9 | **Schedule change proposals** | landed `9e7eda6` | The path back to P6: a refused activity's alternative becomes a proposal the engine has checked (window, knock-on), ledgered; a Proposals view on the Sequence Board; a change-request CSV in P6's import layout; the XER door reports which proposals the next export reflects, so the loop closes where it started. |
| 10 | **A person behind every decision** (S12) | landed `7a12751` (store), `40512dd` (API); `db9acd1`, `86276c5` (shell), `1b4759b` (docs) | The identity hop carries a person: `x-wadl-person` (the proxy's stable subject, required in proxy mode — 401 `"the proxy asserted no person (x-wadl-person)"` without it, 401 outside `[A-Za-z0-9._:@/-]{1,128}` in either mode), `x-wadl-person-name` (percent-encoded, display only, falls back to the id with a `whoami` warning) and `x-wadl-roles` (eight codes; unknown codes ignored and reported; absent → `WADL_DEFAULT_ROLES` or `reader` behind the proxy, every door on the dev shim with the warning `demo mode: no x-wadl-roles — every door is open`). `TenantScope` carries an `Actor { id, name, source }`; every ledger row on both stores is chain format 2 with the actor in the hash (migration 0017), the rows before are format 1 and verify in the same chain, `wadl verify-ledger` reads old and new exports. One role → capability matrix (`roles.rs`: `read · raise_hazard · clear_hazard · commit_document · propose · decide`) and one `route_layer` gate over every POST (23 routes, the yard-clock door included): a role without the capability is refused 403 problem+json `{"detail":"Foreman may not record a clearance — clear_hazard is held by Ship Super and Safety","capability":"clear_hazard","roles":["foreman"]}`, nothing written; a dry run is never gated; a foreign hull is 404 before any capability is judged. `/api/whoami` serves person, roles, capabilities, hulls, the matrix, warnings and `WADL_MARKINGS`; `/health` names the identity mode; the audit line carries `person`. Generated weakest-role tests (one per gated route, as a reader), `identity.rs`, `pg_rls` round trip, SSP AC-6/AU-10/IA-2 statements, WADL-SA-11. **Shell**: boots from `/health` then `/api/whoami` and asserts nothing until it knows the mode — behind the proxy no identity headers, the asserted person and roles shown read-only; on the dev shim a demo person per role (`x-wadl-person: dev:safety`, name percent-encoded, `x-wadl-roles`) so switching role switches what the server permits and who the ledger names, under an amber `DEMO MODE · dev identity` badge. Hull picker from `whoami.hulls` (plus two `not assigned · demo` hulls in demo mode), markings from `whoami.markings` (amber `HANDLING MARKINGS NOT RECEIVED` without them), "identity unavailable" when whoami fails rather than an empty list. `identity.ts` (`can()` reads capabilities, never role names; `refusalSentence()` mirrors `roles::refusal_sentence` from the served matrix; five tests). Every mutating call throws the server's sentence (`ApiRefusal`); every door — clear and raise on the Deck Explorer, the zone chart's commit/discard, Confirm import and every card's Discard on Data Sources (hazard log under `raise_hazard`), the three Propose buttons, Decide/Record on the options panel, acknowledge on Conflicts & Risk — is grey with the sentence as tooltip when the capability is not held, and a 403 that still arrives renders inline. Decisions Ledger: a **By** column (person; `⚙ system:…`; `— before format 2` for chain-1 rows), a header row, the chain format as a stat. Field Guide §07. Docs: `docs/identity-proxy-contract.md` (normative, with the staging test), `deploy/README.md`, POAM-1 evidence and POAM-6 closed, posture Pillar 2 and the control map, README curl with the person headers, the demo script. Deferred: a directory-backed `person` row behind `actor_id`; per-hull roles; the Playwright smoke that S17 owns (this sitting's browser check is manual, screenshots in the session record). |
| 11 | **Survive the yard's XER, with run history** (S13) | sitting A landed `652f1cc`, `5c9d8e1` (ingest), `1bee343` (store + migration); sitting B landed `666793f` (door moved), `84fbf23` (door, field-map door, runs, stable ids, boot, CLI), `ea12b6f` (tests + record); sitting C landed `3495661` (api.ts, ingest.ts words, tests), `1781475`, `3a289b8` (ScheduleDoor on the board, field-map card, breadcrumb, App re-read), `24a33af` (browser-verified, runs table, parser tests), docs this commit | **Sitting C — complete.** The shell. `ScheduleDoor.tsx` replaces the schedule card on Data Sources (rendered wide, through the shared `SourceCard.tsx` extracted from `SourcesBoard.tsx`): pick or drop an XER → `decodeXerFile` (strict UTF-8, else Windows-1252; the panel says *utf-8 (decoded by the browser)*) → dry run → the staged panel: counts, *projects served: X (of N in the file)*, a **Field map** panel whose four selects are built by `fieldChoices` from `fields_seen` (`UDF: COMPT — Location placard (8 rows)`, `Activity code: LOC (1)`, the stored choice kept and marked *not in this file*), a project checkbox list and the placards toggle — every change re-runs the dry run through an inline `field_map` with a stale guard and the panel says *chosen here — committed with the run as this hull's map when it differs from the stored one*; Location / Hours (*labor only: 1 material and 1 equipment assignments not in anyone's man-hours*) / **Quarantine** (grouped by class, folded at 25 with *N more behind this fold · show all*, line · table · code · class · reason) / Excluded (`exclusionSummary`) / Findings / Delta (`deltaSummary`). Confirm is disabled without `commit_document` with the server's refusal sentence on the button and under the bar. The card's name line reads *reading <label>, imported <when> by <person\|org …0001 (no person on record)> · at boot · run #N*; its first line carries quarantined / excluded counts and the encoding. **Runs** fold (read on open and on every board re-read): newest first, *SERVED* marked, *diff vs served* renders the delta as *run #1 vs served run #2 <label>: …*, *serve this run…* is a two-step confirm (*A revert to a prior import: its N rows replace the served schedule on every screen, ledgered as SCHEDULE_REPLACED naming both runs*, disarming after 8 s), capability-gated; a failed read renders *run history unavailable*, never an empty table. Discard on the card reverts to the generated register and says the runs are kept. A **P6 field map** card beside it: DEFAULT / INGESTED, `fieldMapSummary` of the map in effect, the served run's survey, *Upload field-map JSON* (`parseFieldMapJson` refuses a wrong shape naming the slot; the door's dry run adds findings) → Confirm, Discard → default. `Chrome.tsx` `TopBar` gains `scheduleRun: ScheduleRunSummary \| null \| "unavailable"` and a 10.5 px line under the hull crumb from `scheduleCrumb` — *reading CVN73-PIA26-full.xer · imported 09/06 20:13 by org …0001 (no person on record) · at boot* in amber until a person is on the run, green with one, *reading the generated register* dim, *schedule source unavailable* amber on a failed read; `App.tsx` re-reads the timeframe on `dataEpoch` and passes `frame.schedule_run`. Tests: `scheduleDoor.test.ts` (decoder branch on the shared literal and a BOM, `fieldChoices`, the yard words, the fold at 25, the crumb and the run line), `ingest.test.ts` (`parseFieldMapJson` shape and refusals; `deltaSummary` unchanged). Browser-verified on the release build against the yard-shaped fixture: COMPT offered and picked, DSRA unticked → *7 of 8 authored · 1 read from task names*, 3 quarantined with reasons, commit → crumb *by Demo Planner (Y-1001)*, ledger `DOCUMENT_REPLACED p6_field_map` then `SCHEDULE_REPLACED` under the Planner, diff, serve-again of run #1 (`+5706 new · −9 gone · ⚠ 414 newly NOT executable`), Foreman refused with the sentence. Docs: `docs/p6-ingest-schema.md` (field map, encoding, quarantine/exclusion, runs; labor-only hours; multi-project line rewritten), `docs/demo-script.md` §6 rewritten to the door, Field Guide §02 paragraph. Deviations: the fixture has 10 work activities under the default map and 8 under the yard map (the packet's acceptance said 9), so the words are *0 of 10* → *7 of 8*; the schedule door's field-map panel edits the map only while a file is staged — with no file staged the map is edited through the P6 field map card's JSON door, not inline selects (the packet's "edits the stored map through its own door"); the release binary had to be rebuilt for verification (it predated sitting B's door commit). Deferred: nothing from the packet's shell scope. **Sitting B.** The XER door lives in `crates/wadl-api/src/schedule_door.rs` (out of `handlers.rs`, which is 300 lines shorter). It reads the export through the hull's field map — the stored `p6_field_map` document or one sent inline as `field_map` — and serves the file around its quarantine: preview and commit carry `run { encoding, decoded_by, projects_served, counts, field_map, field_map_source: inline\|document\|default }`, `fields_seen`, `quarantine[{line, table, code, class, reason}]`, `exclusions { loe, wbs, project }`, `findings`, beside the reconciliation, mapping, delta, clock and wall-clock findings they carried; a commit adds `run_id` and `seq`. Refused whole (422) only for the parser's cell ceiling, no `TASK` section, no surviving activity (`XER rejected: every one of N TASK rows was quarantined or excluded — line 44: …; …`), a malformed inline map, or an `encoding` hint that is neither `utf-8` nor `windows-1252`. The body carries the export as text: the browser decodes and sends `encoding` (`decoded_by: browser`); a hint-less caller is recorded `utf-8` / `caller`; boot and CLI decode bytes on the server. Every commit writes one run through `commit_schedule_run` on both stores and a `SCHEDULE_REPLACED` line with `run_id`, `seq`, `imported_by`, `encoding`, `decoded_by`, `field_map`, `field_map_source`, `counts` and the delta; an inline map that differs from the stored one (or the default) is committed first as the field-map document with its own `DOCUMENT_REPLACED` line. Field-map door: `GET /field-map` → `{ source, label, map, fields_seen }` (the served run's survey), `POST /field-map[?dry_run=true]` `{ label, map }` → `{ stored, label, map, findings }` (refused whole with every reason; findings against the served survey never refuse), `POST /field-map/revert`; ledgered kind `p6_field_map`. Runs: `GET /schedule-runs` → `{ served, runs[] }` newest first; `GET /schedule-runs/detail?run=` → `{ summary, report }` (404 unknown, 422 unnamed); `GET /schedule-runs/diff?run=&against=` → `{ run, against, delta }` in the door's delta shape, `against` defaulting to the served run; `POST /schedule-runs/serve { run_id }` → `{ served, delta }`, ledgered `SCHEDULE_REPLACED` with `reverted_to_run: true, from_run`. A run whose rows the in-memory cap dropped is a 409 problem naming why. The three new POSTs sit under `commit_document`; 57 scoped routes leak-tested, 60 endpoints in the SSP. Activity ids are stable from (hull, task_code) — `schedule::stable_activity_id`, sha256 with version nibble 8 — so a re-import keeps every row's id; `sha2` added to `wadl-api` from the workspace, no new crate. Every activity row carries `work_type`. `/timeframe` and `/activities` carry `schedule_run` (the served run's summary: label, `imported_at_ms`, `imported_by { org, person, via }`) for the breadcrumb. Boot: `WADL_DEMO_DOCS` loads `*-fieldmap.json` before the export (`reference/cvn73/CVN73-fieldmap.json`, today's convention), `serve.rs` reads the XER as bytes and records run #1 via `boot` with `person: null`; a quarantine prints and the rest is served; banner `schedule of record: CVN73-PIA26-full.xer — 5706 activities, 0 quarantined, utf-8, map CVN73-fieldmap.json · parsed in … · run #1`. CLI `wadl ingest-xer --input f.xer [--survey] [--field-map m.json]`: the survey prints `fields_seen`, encoding and the quarantine as line/table/class, no task code, name or reason. Tests: `crates/wadl-api/tests/schedule_runs.rs` (fourteen: the packet's twelve, the field-map door split in two, plus the 409 on a capped run), `activities.rs` asserts `run_id` on the ledger line. Deviations: `decoded_by` has a third value `caller` for a text body with no hint; `counts.quarantined` under the yard map on the yard-shaped file is 3 (the two bad TASK rows stay quarantined whatever the map; the packet's acceptance said 1), `load_xer` takes the boot instant explicitly like `load_demo_docs`. Deferred to C: the shell (decode in the browser, `ScheduleDoor.tsx`, breadcrumb, `App.tsx` timeframe on `dataEpoch`), `docs/p6-ingest-schema.md` and the demo script. **Sitting A.** The yard's export conventions are data: `wadl_ingest::field_map::FieldMap` (compartment, work item, work type, trade ← a UDF by name or label, an activity code type, the first labor resource, or nothing; the projects to serve; whether placards are read from task names), defaults equal to today's hard-wired names so no hull needs a map to keep working, refusals whole (`resource` outside trade, blank field, duplicate project), findings never refuse. `wadl_ingest::encoding::decode_xer` reads UTF-8 (BOM stripped) or Windows-1252 through a hand-rolled 128-entry WHATWG table, no decoder crate. `ingest_xer_with(input, label, &FieldMap, &YardClock)` is the one parse entry point: rows that cannot be honestly accepted are quarantined with line, table, code and class instead of refusing the file; `TT_LOE` and `TT_WBS` rows are excluded and listed; `TT_FinMile` is a key event; projects the map does not name are excluded with their internal logic and counted; only `RT_Labor` assignments are man-hours (material and equipment skipped and counted, an untyped resource counted as labor with a finding); `fields_seen` surveys the file without schedule content; every activity carries `work_type`. Fixture `reference/p6-sample/CVN73-PIA26-yardshape.xer` (UDF `COMPT`, code `LOC`, two projects, LOE/WBS rows, material/equipment/untyped resources, a cross-project predecessor, a bad date at line 44, a width error at line 45); thirteen new ingest tests. Store: `ScheduleRun { summary, report, doc }` with `ImportedBy`, `RunCounts`, `QuarantinedRow`; `commit_schedule_run` records the run and serves it in one write (assigns `run_id` from the caller's instant and `seq` under the per-hull advisory lock), `list_schedule_runs` newest first, `schedule_run`, `serve_schedule_run`, `served_schedule_run`, and the `p6_field_map` document (`FieldMapDoc { label, map }`) on both stores; `clear_schedule_of_record` keeps the runs; in-memory keeps the rows of the newest 12 runs (`MAX_RUN_DOCS`). Migration `0018_schedule_runs_and_field_map.sql` gives `ingest_run` its writer and `ingested_document` a `run_id`; `pg_rls` proves both round trips in tenant. Deferred to sitting B: the door (quarantine served in the preview, 422 only when no `TASK` section or nothing survives), the field-map door, run routes, stable activity ids from (hull, task_code), boot/CLI; to C: the shell. One deviation from the packet: a slot has one source, so under a `COMPT` map the ninth located row is Medium via the placard in its name, and via `LOC` under an activity-code map. |

After slice 6 the build is demoable end to end on the production store. The
pilot-blocking items that remain from the pilot review (person identity in
the ledger, the P6 field map and quarantine, the rule-set door and the
safety authority's table, the engine at a 3,000-space register, database
operations) are the pilot programme, sequenced in that review.

## What a demo walks through, once the slices land

The scripted version, with every space, activity and hazard named from the
reference hull, is `docs/demo-script.md`. The outline:

1. Open as a Foreman: the shift board for today, the status strip, the
   legend. Print the shift sheet.
2. Switch to Zone Manager: the zone board, worst first; the zone day sheet.
3. Open a held space on the plate: the trace in yard words, the field
   condition holding it, the priced options. Record a clearance with its
   basis; watch every space it held re-derive; scrub back an hour and see
   the hold that was really there.
4. Data Sources: import the yard's schedule with a dry run; the mapping
   report; the re-import delta. Raise today's hazard from the log.
5. Conflicts and Actions: what is worth doing first, and the cascade of
   doing it. Acknowledge an issue; open the ledger and read the chain
   verify.
6. Reports: the conflict log and the field-condition register, cut, dated,
   printed.
