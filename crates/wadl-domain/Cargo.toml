[package]
name = "wadl-domain"
description = "Newtypes, IDs, value objects, and the permit lifecycle typestate. No I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
uuid.workspace = true
serde.workspace = true

[dev-dependencies]
# Reads the shared clock vector file in civil.rs's tests; test-only, so the
# crate's runtime tree (and its wasm build) stays serde + uuid.
serde_json.workspace = true

[lints]
workspace = true
